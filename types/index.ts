export type StoreTypes = {
  name: string;
  postalCode: number;
  storeNo: number;
};

export enum PartEnum {
  UrlFriendlyTerm = "URLTerm",
  Term = "Term",
}

export enum AggregationsEnum {
  Brand = "brand",
}
